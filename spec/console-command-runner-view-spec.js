'use babel';

import ConsoleCommandRunnerView from '../lib/console-command-runner-view';

describe('ConsoleCommandRunnerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
